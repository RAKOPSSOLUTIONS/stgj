<!DOCTYPE html>
<html lang="{{ app.request.getLocale() }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="Reservation STJG">
  <title>{% block title %}STJG Reservation - {{ status_text|trans }}{% endblock %}</title>
  {% block head %}
    {{ encore_entry_link_tags('app') }}
    <style>
      body { 
        background: #000; 
        font-family: Arial, sans-serif;
        color: #fff;
      }
      .container {
        height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
      }
      .logo-container {
        text-align: center;
        margin-bottom: 2rem;
      }
      .logo {
        height: 80px;
        width: auto;
      }
      .status-code { 
        font-size: 8rem; 
        line-height: 8rem; 
        text-align: center; 
        color: #fff;
        font-weight: bold;
        margin-bottom: 1rem;
      }
      .status-text { 
        font-size: 1.8rem; 
        color: #ccc; 
        text-align: center; 
        text-transform: uppercase;
        letter-spacing: 2px;
        margin-bottom: 1.5rem;
      }
      .exception-message { 
        font-size: 1.2rem; 
        color: #aaa; 
        text-align: center;
        max-width: 600px;
        margin: 0 auto 2rem;
        line-height: 1.6;
      }
      .btn-container {
        display: flex;
        justify-content: center;
        gap: 15px;
        margin-top: 2rem;
      }
      .btn {
        padding: 10px 20px;
        border: 1px solid #444;
        border-radius: 4px;
        text-decoration: none;
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        gap: 8px;
      }
      .btn-outline-secondary {
        color: #fff;
        background: transparent;
      }
      .btn-outline-secondary:hover {
        background: #222;
      }
      .btn-outline-primary {
        color: #fff;
        background: #0066cc;
        border-color: #0066cc;
      }
      .btn-outline-primary:hover {
        background: #0055aa;
      }
      .copyright { 
        width: 100%; 
        text-align: center; 
        color: #555; 
        font-size: 12px;
        margin-top: 3rem;
      }
      @media (max-width: 768px) {
        .status-code {
          font-size: 6rem;
          line-height: 6rem;
        }
        .status-text {
          font-size: 1.4rem;
        }
        .exception-message {
          font-size: 1rem;
        }
        .btn-container {
          flex-direction: column;
          align-items: center;
        }
      }
    </style>
  {% endblock %}
</head>
<body>

  <div class="container">
    <div class="logo-container">
      <img src="{{ asset('assets/img/logo-stjg-old.png') }}" alt="STJG Logo" class="logo">
    </div>
    
    <div class="status-code">{{ status_code }}</div>
    <div class="status-text">{{ status_text|trans }}</div>
    
    {% if status_code == 404 %}
      <div class="exception-message">{{ "Désolé, nous n'avons pas pu trouver cette page. La page que vous recherchez peut avoir été déplacée ou n'existe plus."|trans }}</div>
    {% elseif status_code == 403 %}
      <div class="exception-message">{{ "Vous n'êtes pas autorisé à accéder à cette page. Veuillez contacter l'administrateur si vous pensez qu'il s'agit d'une erreur."|trans }}</div>
    {% else %}
      <div class="exception-message">Une erreur est survenue. <span style="display:none; visibility:hidden">{{ exception.message|trans }}</span></div>
    {% endif %}
    
    <div class="btn-container">
      <a href="#" class="btn btn-outline-secondary" onclick="history.go(-1);">
        <i class="bi bi-arrow-left"></i>
        {{ "Retour"|trans }}
      </a>
      <a href="mailto:contact@218labs.ma" class="btn btn-outline-primary">
        <i class="bi bi-envelope"></i>
        {{ "Contactez-nous"|trans }}
      </a>
    </div>
    
    <div class="copyright">
      &copy; {{ 'now'|date('Y') }} STJG Reservation. Tous droits réservés.
    </div>
  </div>

</body>
</html>